replicaCount: 1

image:
  repository: registry.digitalocean.com/k8s-microservice/k8s-microservice
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80
  targetPort: 3000

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "250m"

env:
  NODE_ENV: production
  SERVICE_VERSION: "1.0.0"

imagePullSecret: k8s-microservice